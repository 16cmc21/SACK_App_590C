<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.colincumming.sacapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".QuizActivity" />
        <activity android:name=".famActivity" />
        <activity android:name=".trustActivity" />
        <activity android:name=".avoidActivity" />
        <activity android:name=".fam2Activity" />
        <activity android:name=".adultActivity" />
        <activity android:name=".nothActivity" />
        <activity android:name=".famResActivity" />
        <activity android:name=".ResActivity"></activity>
    </application>

</manifest>